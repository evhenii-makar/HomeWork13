import java.util.Arrays;

/*
 * Created by mag_o on 26.11.2016.
 */
public class main {

    public static void main(String[] args) {

        Masiv proba = new Masiv();
        Integer[] z = {50, 46, 65, 54, 65,5};
        int   nu = 37;


        proba.addIntToArray(nu);
        proba.addArreyToArrey(z);
        proba.addIntToArray(nu);
        proba.avgElement();
        proba.addIntToArray(26);
        proba.minElement();
        proba.maxElement();
        proba.avgElement();






    }

}
